<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Portfolio: Arash Tarafar</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://unpkg.com/spectre.css/dist/spectre.min.css">
    <link rel="stylesheet" href="../../css/reset.css">
    <link rel="stylesheet" href="../../css/styles.css">
    <link rel="stylesheet" href="../../css/page.css">
    <script src="../../js/script.js" defer></script>
</head>
<body>
    <header id="top">
        <div class="hero container" id="about">
            <nav class="topbar">
                <p class="menu-mobile">MENU</p>
                <ul class="menu">
                    <li class="menu-link"><a href="../../">About</a></li>
                    <li class="menu-link"><a href="../../experience">Experience</a></li>
                    <li class="menu-link"><a href="../../projects">Projects</a></li>
                    <li class="menu-link"><a href="../../skills">Skills</a></li>
                    <li class="menu-link"><a href="../../education">Education</a></li>
                    {{#if isLoggedIn}}
                        <li class="menu-link">
                            <a href="../../logout" class="menu-user">Log out {{#unless isAdmin}}({{ name }}){{/unless}}</a>
                            {{#if isAdmin}}
                                <span>/ </span>
                                <a href="../../users" class="dashboard">Users</a>
                            {{/if}}
                        </li>
                    {{else}}
                        <li class="menu-link"><a href="../../login" class="menu-user">Log in</a></li>
                    {{/if}}
                </ul>
                <div class="darkmode-toggle">
                    <i class="bi bi-brightness-high-fill toggle-icon"></i>
                </div>
            </nav>            
        </div>
    </header>
    <main>
        <section class="section-entry">
            <h2>MODIFY EXPERIENCE</h2>
            {{#each experience}}
                <form action="./{{ experienceID }}" method="POST">
                    <select name="organization" id="organization">
                        <option value="{{ organizationID }}">{{ organizationName }}</option>
                    </select>
                    <input type="text" name="experienceTitle" id="experienceTitle" value="{{ experienceTitle }}" placeholder="Position">
                    <input type="text" name="experienceDate" id="experienceDate" value="{{ experienceDate }}" placeholder="Start/End Dates">
                    <textarea name="experienceDescription" id="experienceDescription" placeholder="Job Description" cols="30" rows="10">{{ experienceDescription }}</textarea>
                    <input type="submit" value="SAVE EXPERIENCE">
                </form>
            {{/each}}
        </section>
    </main>
    <footer class="container">
        <div class="columns">
            <div class="contacts col-12">
                <span class="prompt">CONTACT ME:</span> <span class="phone">+46 070 9678 449</span> <span class="email">arashtarafar@gmail.com</span> <a href="https://www.linkedin.com/in/arashtarafar/" target="_blank" class="linkedin">linkedin.com/in/arashtarafar</a>
            </div>
            <a class="backtotop" href="#top">↑</a>
        </div>
        <p class="copyright">All Rights Reserved. Arash Tarafar, 2023</p>
    </footer>
</body>
</html>